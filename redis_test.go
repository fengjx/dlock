package dlock_test

import "testing"

func TestLock(t *testing.T) {
	testCases := []struct {
		name string
	}{
		{
			name: "test",
		},
	}
	for _, tc := range testCases {
		t.Run(tc.name, func(t *testing.T) {

		})
	}
}
